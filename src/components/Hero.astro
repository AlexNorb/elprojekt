---
import ArrowRightIcon from "./icons/ArrowRightIcon.astro";

import { pb } from "../utils";
---

<section
    id="home"
    class="relative min-h-screen flex items-center pt-20 pb-16 lg:pb-24 overflow-hidden bg-slate-900"
>
    {/* Background Image & Overlay */}
    <div class="absolute inset-0 z-0">
        <img
            src="public/images/hero/hero-left.webp"
            alt="Modern Technology Background"
            class="w-full h-full object-cover opacity-100"
        />
        <div
            class="absolute inset-0 bg-gradient-to-b from-white/70 via-white/40 to-white/0"
        >
        </div>
    </div>

    <div
        class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 w-full pt-16 flex flex-col items-center justify-center text-center"
    >
        <h1
            class="text-4xl tracking-tight font-extrabold text-slate-900 sm:text-5xl md:text-6xl lg:text-7xl font-display leading-tight animate-fade-in-up max-w-4xl"
            style="animation-delay: 0.2s"
        >
            Framtidens El. <br />
            <span class="text-elblue">Personligt levererad.</span>
        </h1>

        <p
            class="mt-8 text-xl text-slate-900 leading-relaxed font-sans max-w-2xl mx-auto animate-fade-in-up opacity-0"
            style="animation-delay: 0.4s; animation-fill-mode: forwards"
        >
            Din trygga partner för solceller, laddboxar och elinstallationer på
            Tjörn, i Stenungsund och Kungälv. Vi levererar hållbara ellösningar
            med personlig service och högsta kvalitet.
        </p>

        <div
            class="mt-10 flex flex-col sm:flex-row gap-4 justify-center animate-fade-in-up opacity-0"
            style="animation-delay: 0.6s; animation-fill-mode: forwards"
        >
            <a
                href={pb("/#contact")}
                class="group relative inline-flex items-center justify-center px-8 py-4 text-base font-bold text-white transition-all duration-200 bg-elblue font-display rounded-md hover:bg-elblue-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-elblue shadow-[0_4px_14px_0_rgba(0,118,255,0.39)] hover:shadow-[0_6px_20px_rgba(0,118,255,0.23)] hover:-translate-y-1"
            >
                Begär Offert
                <div
                    class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform inline-block"
                >
                    <ArrowRightIcon />
                </div>
            </a>

            <a
                href="#services"
                class="group relative inline-flex items-center justify-center px-8 py-4 text-base font-bold text-slate-700 transition-all duration-200 bg-white/50 border border-slate-200 font-display rounded-md hover:bg-white hover:text-elblue hover:border-elblue/30 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-slate-200 backdrop-blur-sm"
            >
                Våra Tjänster
            </a>
        </div>

        {/* Glow effects - Centered */}
        <div
            class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-elblue rounded-full mix-blend-screen filter blur-[120px] opacity-20 animate-pulse-slow pointer-events-none -z-10"
        >
        </div>
    </div>
</section>

<style>
    /* Simple float animation if not in global css yet */
    @keyframes float {
        0% {
            transform: translateY(0px);
        }
        50% {
            transform: translateY(-20px);
        }
        100% {
            transform: translateY(0px);
        }
    }
    .animate-float {
        animation: float 6s ease-in-out infinite;
    }
    .animate-pulse-slow {
        animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
</style>
